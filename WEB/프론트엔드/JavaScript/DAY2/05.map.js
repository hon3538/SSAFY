
const array =[1,2,3,4,5,6]; //original array
const result=array.map(data=>data); //data=>return data;
//it's same result with array

const result2=array.map(data=>data+1);

//Challenge! utilizing map
const arr =[1,2,3,4,5];
const square=arr.map(i=>i*i);
//get square

const arr2 =["a","bcd",'ef',"g"]; 
const length=arr2.map(i=>i.length);
//get length of str