#include <iostream>
using namespace std;

/*
각 level의 노드 개수 파악, 같은 레벨에 중복 배치 불가능 하기 때문
경우의 수 = (각 level의 노드 개수 + 1)를 모든 곱한 수 - 1
+1은 해당 레벨에 차량이 아예 배치되지 않는 경우이고, 마지막 -1은 모든 차량이 배치되지 않는 경우는 없기 때문이다.


*/
int main(){




    return 0;
}