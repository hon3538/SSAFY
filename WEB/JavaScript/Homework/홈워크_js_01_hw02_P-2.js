
// `map`
    
// `filter`

// `find`

// `every`

// `some`

// `reduce`

const nums = [1, 2, 3, 4];

(x,y)=>{
    return x+y;
}
const tom = {
    name: 'Tom',
    introduce:()=>{ console.log('Hi, my name is' + this.name) }
  }